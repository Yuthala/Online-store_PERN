const Router = require('express');
const router = new Router();

//метод для создания девайса
router.post('/',)
//метод для получения всех девайсов
router.get('/',)
//метод для получения конкретного отдельного девайса (после перехода на страницу отдельной информации)
router.get('/:id',)

module.exports = router;